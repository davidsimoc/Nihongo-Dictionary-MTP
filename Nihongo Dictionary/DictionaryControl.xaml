<UserControl x:Class="Nihongo_Dictionary.DictionaryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Nihongo_Dictionary"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="18" />
        </Style>
        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#D32F2F" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="BorderBrush" Value="#B0B0B0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FontSize" Value="20" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <DataGrid x:Name="dgKanjiList"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  RowBackground="#1A1A1A"
                  BorderBrush="#B0B0B0"
                  BorderThickness="1"
                  Foreground="#E5E5E5"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nr."
                                    Binding="{Binding Index}"
                                    Width="auto" />
                <DataGridTextColumn Header="Kanji"
                                    Binding="{Binding Kanji}"
                                    Width="*" />
                <DataGridTextColumn Header="Onyomi Reading"
                                    Binding="{Binding Onyomi}"
                                    Width="*" />
                <DataGridTextColumn Header="Kunyomi Reading"
                                    Binding="{Binding Kunyomi}"
                                    Width="*" />
                <DataGridTextColumn Header="Meaning"
                                    Binding="{Binding Meaning}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
